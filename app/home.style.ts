import { tv } from 'tailwind-variants';

export const homeLayoutContainer = tv({
  base: ['w-dvw', 'h-dvh', 'flex', 'flex-col', 'bg-white', 'dark:bg-black'],
});
export const homeContainer = tv({
  base: ['flex-1'],
});
